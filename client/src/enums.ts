export enum MenuRoutes {
  Home = '/',
  News = 'news',
  SingleNews = 'news/:newsId',
  Documents = 'documents',
  Contacts = 'contacts',
}

export enum SystemRoutes {
  SignIn = 'sign-in',
  Add = 'add',
}
