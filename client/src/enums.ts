export const enum MenuRoutes {
  Home = '/',
  News = 'news',
  SingleNews = 'news/:newsId',
  Documents = 'documents',
  Contacts = 'contacts',
}

export enum SystemRoutes {
  SignIn = 'sign-in',
  Add = 'add',
}

export enum AlertType {
  Error = 'error',
  Warning = 'warning',
  Info = 'info',
  Success = 'success',
}
